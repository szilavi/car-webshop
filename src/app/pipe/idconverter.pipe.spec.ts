import { IdconverterPipe } from './idconverter.pipe';

describe('IdconverterPipe', () => {
  it('create an instance', () => {
    const pipe = new IdconverterPipe();
    expect(pipe).toBeTruthy();
  });
});
